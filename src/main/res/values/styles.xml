<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 进度条 -->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimAmount">0.8</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- 对话框 -->

    <style name="customDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/bg</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 标题 -->
    <style name="tipText" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:singleLine">true</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:scrollHorizontally">true</item>
    </style>

    <!-- 竖向隔离线 -->
    <style name="vsplitView" parent="android:style/Widget.TextView">
        <item name="android:layout_width">@dimen/vSplitWidth</item>
        <item name="android:layout_height">@dimen/vSplitHeight</item>
        <item name="android:background">@color/hint</item>
    </style>

    <!-- 横向隔离线 -->
    <style name="hsplitView" parent="android:style/Widget.TextView">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/hSplitHeight</item>
        <item name="android:background">@color/hint</item>
    </style>

    <!-- 单行编辑框 -->
    <style name="singleLineEdit" parent="@android:style/Widget.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/editHeight</item>
        <item name="android:paddingLeft">@dimen/splitMargin</item>
        <item name="android:background">@color/white</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/hint</item>
        <item name="android:textSize">@dimen/editTextSize</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>